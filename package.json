{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "postinstall": "lerna bootstrap",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "coverage": "lerna run coverage --stream",
    "coverage:report": "lerna run codecov --stream",
    "docs": "lerna run docs --stream && ./scripts/copy-docs.sh",
    "prep:release": "npm run build && npm run docs && lerna run package:preview --stream",
    "release": "lerna publish",
    "version:prerelease": "lerna version --conventional-prerelease"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "lerna": "^3.16.4",
    "prettier": "^1.18.2"
  }
}
